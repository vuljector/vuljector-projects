name: Project structure

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate layout
        run: python3 scripts/validate_layout.py --root "${GITHUB_WORKSPACE}"
